# 个人极简面试官 - 提交材料汇总

## 📋 提交内容清单

### 1. 代码仓库链接

**GitHub仓库**: [https://github.com/zhousirui0714/mianshiguan](https://github.com/zhousirui0714/mianshiguan)

### 2. 日志链路说明

#### 三层JSON日志结构

系统实现了**进入/步骤/出口三层日志**，格式如下：

**ENTRY（进入日志）**
```json
{
  "timestamp": "2024-01-15T10:30:00Z",
  "level": "INFO",
  "request_id": "uuid-xxx",
  "stage": "ENTRY",
  "endpoint": "/api/questions/generate",
  "method": "POST",
  "user_id": "user-xxx",
  "params": {...},
  "message": "Entering endpoint"
}
```

**STEP（步骤日志）**
```json
{
  "timestamp": "2024-01-15T10:30:01Z",
  "level": "DEBUG",
  "request_id": "uuid-xxx",
  "stage": "STEP",
  "step": "call_llm",
  "status": "IN_PROGRESS",
  "details": {...}
}
```

**EXIT（出口日志）**
```json
{
  "timestamp": "2024-01-15T10:30:05Z",
  "level": "INFO",
  "request_id": "uuid-xxx",
  "stage": "EXIT",
  "endpoint": "/api/questions/generate",
  "success": true,
  "latency_ms": 5000,
  "result": {...}
}
```

#### 日志用途

| 阶段 | 用途 |
|------|------|
| **ENTRY** | 记录请求入口，便于追踪请求来源和参数 |
| **STEP** | 记录处理过程，便于定位问题发生在哪个环节 |
| **EXIT** | 记录请求结束，便于统计耗时和成功率 |

### 3. Evals评估结果

#### 统计表格

| 指标 | 数值 |
|------|------|
| **总测试次数** | 15 |
| **格式合规率** | 13.3% (2/15) |
| **平均内容准确率** | 10.3/100 |
| **最高内容评分** | 77/100 |
| **最低内容评分** | 0/100 |

#### 评估说明

- **格式合规率**：检查返回是否为有效JSON且包含正确结构（3个问题，每个问题有question字段）
- **内容准确率**：评估问题质量（长度、深度、追问逻辑完整性）
- **改进方向**：当前模拟数据包含多种错误情况，真实接入LLM API后应重新评估

### 4. 韧性保护方案

#### Timeout配置

| 配置项 | 值 | 选择依据 |
|--------|-----|----------|
| **LLM API超时** | 20秒 | 大模型生成3条追问通常需5-15秒，预留网络延迟 |
| **重试次数** | 2次 | 避免无限重试，快速降级 |
| **降级策略** | 预设问题库 | 按技术栈分类（前端/后端/通用） |

#### 降级问题库分类

| 类别 | 适用场景 | 示例问题 |
|------|----------|----------|
| **前端** | React/Vue/JS技术栈 | 性能优化、状态管理、响应式设计 |
| **后端** | Java/Python/Go技术栈 | 数据库优化、缓存策略、高并发处理 |
| **通用** | 其他技术栈 | 项目难点、架构设计、复盘能力 |

### 5. ADR-2：LLM API调用采用Timeout + 降级方案

#### 背景

系统核心依赖外部大模型API生成刁钻追问，但大模型API存在响应不稳定、偶尔超时、限流等问题。真实用户场景下，API故障会导致整个系统不可用，严重影响用户体验。

#### 决策

选择 **Timeout + 降级方案**：设置20秒超时，失败时使用预设问题库返回。

#### 理由

1. **用户体验保障**：即使LLM API故障，用户仍能获得预设的高质量追问问题
2. **系统稳定性**：避免因外部依赖故障导致整个系统不可用
3. **超时时间合理**：平衡响应速度和成功率
4. **降级问题库分类**：根据技术栈提供针对性问题

#### 代价

1. **降级问题不够个性化**：预设问题无法针对具体项目描述生成
2. **额外维护成本**：需要持续维护和更新预设问题库
3. **用户感知差异**：降级时用户可能感觉问题质量有所下降

### 6. 项目结构

```
mianshiguan/
├── src/                    # 源代码目录
│   ├── models/             # 数据模型定义
│   │   └── __init__.py
│   ├── services/           # 业务服务层
│   │   ├── llm_client.py   # LLM客户端（超时+降级）
│   │   └── interview_service.py  # 核心业务服务
│   └── utils/              # 工具类
│       └── logger.py       # JSON日志工具
├── prd.md                  # PRD文档（含ADR-1和ADR-2）
├── database-design.md      # 数据库设计文档
├── evals.py                # Evals评估脚本
├── evals_results.json      # Evals评估结果
├── main.py                 # 测试入口
└── requirements.txt        # 依赖配置
```

### 7. 核心功能

| 功能 | 说明 |
|------|------|
| **简历解析** | 支持PDF上传，自动提取项目经历模块 |
| **RAG检索** | 构建个人简历向量知识库 |
| **追问生成** | 模拟大厂面试官风格，生成3条刁钻追问 |
| **韧性保护** | Timeout+降级，确保系统稳定运行 |

---

*提交人：zhousirui0714*  
*提交日期：2026-05-25*  
*项目：个人极简面试官（简历PDF项目刁钻追问）*