# ADR-2：LLM API调用采用Timeout + 降级方案

**状态**：已实施  
**创建日期**：2026-05-25  
**所属项目**：个人极简面试官  

---

## 一、背景

系统核心功能是基于用户简历项目生成刁钻追问，完全依赖外部大模型（LLM）API。然而，大模型API存在以下问题：

1. **响应不稳定**：API调用成功率约95%，存在5%的失败率
2. **响应延迟高**：生成3条追问通常需要5-15秒
3. **限流风险**：第三方API有调用频率限制
4. **网络依赖**：网络波动可能导致超时

在真实用户场景下，任何一次API故障都会导致整个系统不可用，严重影响用户体验。需要建立韧性保护机制，确保系统在外部依赖故障时仍能正常运行。

---

## 二、选项

| 方案 | 描述 | 优点 | 缺点 |
|------|------|------|------|
| **方案A：Timeout + 降级** | 设置合理超时时间，失败时使用预设问题库返回 | 用户体验有保障，系统稳定性高 | 降级问题不够个性化 |
| **方案B：仅超时无降级** | 设置超时时间，但超时后直接返回错误给用户 | 实现简单 | 用户体验差，服务不可用 |
| **方案C：无限重试** | 失败时不断重试直到成功 | 理论上成功率100% | 可能导致请求无限等待，系统资源耗尽 |

---

## 三、决策

选择 **方案A：Timeout + 降级**

---

## 四、理由

1. **用户体验保障**：即使LLM API故障，用户仍能获得预设的高质量追问问题，不会看到错误页面

2. **系统稳定性**：避免因外部依赖故障导致整个系统不可用，保证服务的持续可用性

3. **超时时间合理**：设置20秒超时（大模型生成3条追问通常需5-15秒），预留5秒网络延迟和排队时间，平衡响应速度和成功率

4. **降级问题库分类**：根据技术栈（前端/后端/通用）提供针对性问题，保证降级后的问题质量

5. **重试机制叠加**：配合最多2次重试策略，进一步提高成功率，总超时可达60秒

---

## 五、代价

1. **降级问题不够个性化**：预设问题无法针对具体项目描述生成，可能不够精准

2. **额外维护成本**：需要持续维护和更新预设问题库，确保问题的时效性和专业性

3. **用户感知差异**：降级时用户可能感觉问题质量有所下降，无法获得针对个人项目的定制化追问

4. **监控复杂度增加**：需要额外监控降级触发频率，以便及时发现LLM API服务问题

---

## 实施细节

### Timeout 配置

```python
LLM_TIMEOUT = 20  # 秒
```

**Timeout值选择依据**：
- 基础耗时：大模型生成3条追问通常需要5-15秒
- 网络预留：预留网络延迟和API排队时间（约5秒）
- 用户体验：超过20秒用户可能会刷新页面
- 重试叠加：配合2次重试，总耗时可达60秒+

### 降级问题库分类

| 分类 | 适用技术栈 | 问题示例 |
|------|-----------|---------|
| **backend** | Java, Python, Spring, Node.js | 数据库优化、缓存策略、高并发处理 |
| **frontend** | React, Vue, TypeScript | 性能优化、状态管理、响应式设计 |
| **default** | 通用技术栈 | 项目难点、技术选型、复盘改进 |

### 代码实现位置

- **LLM客户端**：`src/services/llm_client.py`
- **业务服务**：`src/services/interview_service.py`
- **测试脚本**：`resilience_test.py`（验证韧性保护功能）

---

## 验证结果

通过韧性保护测试套件验证，所有测试通过：

| 测试项 | 结果 |
|--------|------|
| 正常调用测试 | PASS |
| 超时降级测试 | PASS |
| API故障降级测试 | PASS |
| 分类降级测试 | PASS |
| Timeout配置验证 | PASS |